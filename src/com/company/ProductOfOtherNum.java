package com.company;import java.util.ArrayList;/** * Created by Hoshiko on 11/13/15. */    /*        You have a list of integers, and for each index you want to find the product of every integer        except the integer at that index.        Write a function get_products_of_all_ints_except_at_index() that takes a list of integers and        returns a list of the products.        For example, given:          [1, 7, 3, 4]        your function would return:          [84, 12, 28, 21]        by calculating:          [7*3*4, 1*3*4, 1*7*4, 1*7*3]        Do not use division in your solution.     */public class ProductOfOtherNum {    public static ArrayList<Integer> getProductsOfAllExceptAtIndex(int [] list){        int N = list.length;        ArrayList<Integer> productList = new ArrayList<Integer>();        for(int i = 0 ; i < N ; i++){            System.out.println("list[i] " + list[i]);//            System.out.println("numAtIndex " + numAtIndex);            int product = 1;            int j;                for(j = 0; j < N; j++)                {                    if(i!=j)                    {                    product = product * list[j];                    System.out.println("PRODUCTS: " + product);                    }                }            productList.add(product);        }        return productList;    }}